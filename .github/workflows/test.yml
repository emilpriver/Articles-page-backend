name: Django TEST

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag django:$(date +%s)
    - name: Pylint
      run: docker run django:$(date +%s) pylint --load-plugins pylint_django ../backend